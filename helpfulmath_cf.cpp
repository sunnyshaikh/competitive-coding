#include <bits/stdc++.h>
using namespace std;

string helpful_math(string str)
{
    if(str.size() == 1) return str;
    int len = ceil(str.size()/2);
    char ch[len];

    for(int i=0; i<str.size(); i+=2)
    {
        
    }

    return "";
    //
    // 1+2+2+3  
}

int main()
{
    string str;
    cin >> str;

    cout << helpful_math(str) << endl;
}